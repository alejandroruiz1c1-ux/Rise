// ========== ANIMACIONES ==========\n\nconst Animations = {\n  /**\n   * Fade in/out\n   */\n  fade(element, show = true, duration = 300) {\n    return new Promise((resolve) => {\n      element.style.transition = `opacity ${duration}ms`;\n      \n      if (show) {\n        element.style.opacity = '1';\n        setTimeout(resolve, duration);\n      } else {\n        element.style.opacity = '0';\n        setTimeout(resolve, duration);\n      }\n    });\n  },\n\n  /**\n   * Slide up\n   */\n  slideUp(element, duration = 400) {\n    return new Promise((resolve) => {\n      element.style.transition = `transform ${duration}ms, opacity ${duration}ms`;\n      element.style.transform = 'translateY(-20px)';\n      element.style.opacity = '0';\n      setTimeout(resolve, duration);\n    });\n  },\n\n  /**\n   * Slide down\n   */\n  slideDown(element, duration = 400) {\n    return new Promise((resolve) => {\n      element.style.transition = `transform ${duration}ms, opacity ${duration}ms`;\n      element.style.transform = 'translateY(20px)';\n      element.style.opacity = '0';\n      setTimeout(resolve, duration);\n    });\n  },\n\n  /**\n   * Scale effect\n   */\n  scale(element, factor = 0.95, duration = 150) {\n    return new Promise((resolve) => {\n      element.style.transition = `transform ${duration}ms`;\n      element.style.transform = `scale(${factor})`;\n      setTimeout(() => {\n        element.style.transform = 'scale(1)';\n        resolve();\n      }, duration);\n    });\n  },\n\n  /**\n   * Shake animation for errors\n   */\n  shake(element) {\n    element.style.animation = 'shake 0.5s';\n    setTimeout(() => {\n      element.style.animation = '';\n    }, 500);\n  },\n\n  /**\n   * Pulse animation\n   */\n  pulse(element) {\n    element.style.animation = 'pulse 0.6s';\n    setTimeout(() => {\n      element.style.animation = '';\n    }, 600);\n  }\n};